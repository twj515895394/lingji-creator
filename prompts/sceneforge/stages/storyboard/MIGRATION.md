# MIGRATION.md

- Source skill: `scene-storyboard-director` (scene_forge repo).
- Input boundary → `context-policy.yaml` (design, performance, script).
- References not loaded at runtime; use `prompts/sceneforge/assets` via selectedAssetIds.