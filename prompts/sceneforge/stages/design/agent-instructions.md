# Design Stage Agent Instructions

- Call `scene_get_stage_context` before drafting.
- Submit artifacts through `scene_submit_stage_draft`.
- Do not write `project.json`, `sceneforge/state.json`, or `sceneforge/artifact_manifest.yaml` directly.
- Do not scan `.agents/skills` or choose runtime rules yourself.
