Status: completed

## 父问题

`.scratch/sceneforge-studio-p0/PRD.md`

## 要构建什么

全量 `tests/sceneforge-*.test.ts`、`tests/electron-api.test.ts`、`npx tsc --noEmit`；更新 `.handoff/` 记录 P0 完成项与人工验收步骤（两种 entryPath、13 阶段侧栏、UI 对齐清单）。

## 验收标准

- [ ] 上述命令通过
- [ ] handoff 文档更新
- [ ] PRD 超出范围项未误标为已完成

## 被阻塞于

- 02–10（11 若做则含 11）