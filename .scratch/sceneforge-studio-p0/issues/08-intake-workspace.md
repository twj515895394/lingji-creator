Status: completed

## 父问题

`.scratch/sceneforge-studio-p0/PRD.md`

## 要构建什么

`IntakeWorkspace`：源类型、链接/本地入口占位、展示 runner/MCP 状态；当存在改编方向列表时展示只读卡片；`adaptation_selection` pending 时突出「须选择方向」阻塞态（P0 选择可写 manual artifact 或只读+文档说明 Agent 路径二选一，须在 issue 实现说明中写明采用哪种）。

## 验收标准

- [ ] 选中 source_intake 显示专用工作区
- [ ] 阻塞态可见且诚实（无假提交）
- [ ] 与 entryPath intake 创建的项目可进入该工作区

## 被阻塞于

- 06
- 07