Status: completed

## 父问题

`.scratch/sceneforge-studio-p0/PRD.md`

## 要构建什么

新增 `scene-stage-capabilities` 与 `scene-entry-path`：每阶段就绪标签（Studio / MCP / 未实现）、工作区模板 id、entryPath 下推荐起点与依赖阻塞文案。

## 验收标准

- [ ] 选中 stage 可解析 `workspaceTemplate`
- [ ] `entryPath: source_intake` 与 `topic_gate` 阻塞/推荐逻辑有单测
- [ ] 能力表与 PRD 诚实反馈一致（core 三阶段 Studio 就绪）

## 被阻塞于

- 04