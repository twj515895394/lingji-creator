# SceneForge 交接 — Direct LLM 已通、进入「补全后置 + 分阶段真机抠细节」

> 生成：2026-06-18 21:22:56（本地）  
> 分支：`codex/sceneforge-studio-core`  
> 用户口径：**LLM 已通**；有 Direct LLM 的阶段在配置正确时可 **Run → 审阅 → 提交 → Validate → Continue** 走完。  
> **下一阶段策略：** 先由代理把「能跑通但仍后置/半成品」的能力 **补全与优化**；用户再 **按阶段逐个真机测试**，扣细节、慢修。

---

## 0. 给下一代理的第一件事

1. **读本文 §4「补全与优化 backlog」**，按优先级开工（用户明确要求：后置功能尽量先补全，再让用户测）。  
2. **不要**再让用户手抄 `.scratch/patches`（§0.1 旧债已清，见 §2.1）。  
3. 改 `electron/sceneforge/**` 或 `electron/pipeline/**` 后提醒用户 **重启 Electron dev**。  
4. 未经用户同意 **不提交 Git**；可先跑 §6 验证命令。

---

## 1. 产品边界（不变）

| 主题 | 引用 |
| --- | --- |
| 用户可见名 | **视频内容创作工坊**（SceneForge） |
| 工程约束 | `docs/sceneforge/2026-06-17-sceneforge-engineering-constraints.md`、`CLAUDE.md` § SceneForge |
| Phase 3 总路线图 | `docs/sceneforge/2026-06-18-sceneforge-phase3-roadmap.md` |
| Continue vs 自动跑 | **Continue = 审批并切阶段**；**Continue & Run** = 显式单步（`docs/adr/0003`、`.scratch/sceneforge-continue-and-run/`） |
| Runner 写盘 | Run 只出 **草案**；正式产物必须 **`sceneSubmitStageDraft`** |

**Direct LLM 覆盖阶段（能力表）：** reference, story, assets, design, script, performance, storyboard, audio, video_prompts。  
**无 Direct LLM：** source_intake, topic_gate, publish, export（设计如此）。

**ACP Agent（App 内）：** 非本次「LLM 跑通」主路径；core 三阶段仍为 **简报占位**（`scene-acp-stage-runner.ts`）；真 Agent 单轮实现见 `scene-acp-agent-runner.ts` **未接 factory**。

---

## 2. 本会话相对旧交接（`handoff-20260618-183200.md`）的新增/已清项

### 2.1 已清除的旧代码债

| 项 | 状态 |
| --- | --- |
| `script` → `DIRECT_LLM` 能力表 | ✅ `src/sceneforge/lib/scene-stage-run-capabilities.ts` |
| `tests/sceneforge-continue-run.test.ts` | ✅ 全绿 |
| Support Pack Task 5–6 + context policy 注册 script/performance | ✅ |
| Flow Hardening Issue 01 **StageRunPanel 接线** | ✅ `sceneGetStageContext`、缺 required 禁用 Run、默认 Direct LLM、Runner 过滤 |
| `scene-context-builder` required + `satisfied` | ✅；`runStage` 双保险 `SceneRunBlockedError` |
| LLM 设置与 Cut 主链路对齐 | ✅ `resolveDefaultLlmBinding`、`loadFullHeadlessAISettings`、`formatLlmInvokeErrorMessage`、direct_llm 前置校验 |
| 自动化测试 | ✅ 近期口径：`tests/sceneforge-*.test.ts(x)` **216+** 全绿、`npx tsc --noEmit` 通过（以接棒时 vitest 为准） |

### 2.2 用户已验证（维护者口述）

- **设置 → AI** 配置正确后，工坊 **Direct LLM 可真实调用**；404 模型名类问题已通过设置/错误文案路径排查（见 `provider-utils.formatLlmInvokeErrorMessage`）。

### 2.3 工作区状态

- 大量 `M` 未提交：Gate HITL、Continue、Support、Flow Hardening、LLM 修复、测试、`.scratch` 计划勾选同步等。  
- **建议提交分组**见 §7；**默认不提交** `.planning/`。

---

## 3. 当前「能跑通」与「仍后置」对照（LLM 视角）

### 3.1 已具备（工程 + 用户 LLM 通）

- Stage Pack + `scene-direct-llm-runner` + `StageRunPanel` + `SceneRunDraftReview`  
- 支撑链 `ScenePrepSupportWorkspace` + Core 链 `StageRunPanel`  
- Required 上游阻塞（UI + `service.runStage`）  
- 语义 validator（script/performance/assets/audio 等，见 Flow Hardening）  
- Continue & Run（跨阶段 `initialRunResult`）  
- Gate/Intake **卡片式 HITL**（组件在树上有未提交改动）

### 3.2 仍后置 / 半成品（**下一代理优先补全**，§4）

- App 内 **ACP** 与能力表不一致（support 阶段选 ACP 仍 stub/unsupported）  
- **项目 Style / `selectedAssetIds` 选择器**（policy 可注入，Studio UI 不完整）— issue 10  
- **Regenerate / Request Revision** 接 Studio  
- **publish** 专用工作区 + Pack（现为占位）  
- **Core MVP 占位一键填充** 与真 LLM 并存（易误导；可优化文案/折叠/仅 dev）  
- **Continue 产品文案**（继续 ≠ 自动 AI）  
- **Issue 20**：Continue 后 Renderer 未即时切阶段（有复现再修）  
- **Gate HITL Task 5**：旧项目 intake/topic_gate 回显（人工/回归）  
- **真机验收清单** Issue 07 / Core 04（文档化，非阻塞开发）  
- Phase 3 **明确后置**：一键全链、ACP 多轮、ZIP、富文本 script 编辑器等（路线图 §6）

---

## 4. 补全与优化 backlog（下一代理执行顺序建议）

用户诉求：**先把能想到的后置项补全优化，再分阶段真机抠细节。** 建议按包立项，避免单 PR 无限膨胀。

### P0 — 与「LLM 全链体验」直接相关（先做）

| # | 工作项 | 目标 | 主要触点 | 参考 |
| --- | --- | --- | --- | --- |
| P0-1 | **真机 E2E 清单（文档）** | 用户按表测 reference→video_prompts；每阶段 required 依赖、设置检查项 | 新建 `.scratch/sceneforge-direct-llm-e2e/CHECKLIST.md` | Issue 07、Core 04 |
| P0-2 | **Continue & Run 缺上游时** | 下一阶段 Run 前同样阻塞/提示（与 StageRunPanel 一致） | `useSceneStageContinuation`、`SceneForgeStudio` | Flow Hardening issue 01 §3 |
| P0-3 | **Core MVP 占位 UX** | 降低与 Direct LLM 混淆：文案、折叠、或「仅测试流程」次级入口 | `SceneCoreMvpPlaceholder.tsx`、`SceneForgeStudio` | P5 backlog |
| P0-4 | **Runner 下拉与能力表** | intake/gate 不展示 Direct LLM（已部分做）；复核全阶段 | `StageRunPanel` | 能力表 |
| P0-5 | **提交分组 + 计划文档同步** | `.scratch/**/IMPLEMENTATION-PLAN` 与代码一致；准备 commit | git | §7 |

### P1 — 后置功能补全（产品完整度）

| # | 工作项 | 目标 | 参考 |
| --- | --- | --- | --- |
| P1-1 | **Style / selectedAssetIds 选择器** | Studio/IPC 选风格资产，注入 `getStageContext` | `.scratch/sceneforge-studio/issues/10-scene-asset-library-and-style-profiles.md`、Phase 3 P2 |
| P1-2 | **Regenerate / Request Revision** | Studio 按钮接已有 service 能力 | Phase 3 P3、P5 backlog |
| P1-3 | **Publish Stage 工作区** | 非仅 Markdown 占位；独立 Pack 若需则立项 | next-batch / gap |
| P1-4 | **Continue 产品文案** | 区分 Continue / Continue & Run / 不自动 submit | P5 backlog |
| P1-5 | **Gate HITL 旧项目回显** | Task 5 验收 + 必要时补测试 | `.scratch/sceneforge-gate-intake-card-hitl/IMPLEMENTATION-PLAN.md` |

### P2 — ACP（**待做，当前明确不推进**）

| # | 工作项 | 目标 | 参考 |
| --- | --- | --- | --- |
| P2-1 | **Factory 接 `scene-acp-agent-runner`** | support/core 与测试一致；替换 core 简报 stub 或分阶段策略 | `scene-stage-runner-factory.ts`、`scene-acp-stage-runner.ts`、`.scratch/sceneforge-flow-hardening/issues/04-*` |
| P2-2 | **Studio ACP 文案** | 未配置/简报模式说明与 MCP 外链路径 | `StageRunPanel`、MCP `scene_run_stage` |

> 2026-06-18 用户最新口径：先**不做 ACP Studio Alignment**，把它保留为待做项。当前优先级是把 **基于 LLM 的完整流程** 全都完善好，包括前后端、交互、流程运转与细节体验。

### P3 — 用户分阶段真机测试（**维护者执行**，代理辅助修 bug）

按 **P0-1 清单** 顺序；每阶段记录：产物 keys、Validate 结果、语义失败文案、上下文警告。  
代理收到反馈后：**单阶段单 issue**，带复现步骤 + 日志，避免大范围重构。

---

## 5. 计划内文档与验收门（索引，不重复正文）

| 工作包 | 实施计划 | 人工/Electron 验收 |
| --- | --- | --- |
| Core LLM Happy Path | `.scratch/sceneforge-core-llm-happy-path/IMPLEMENTATION-PLAN.md` | `issues/04-core-llm-electron-acceptance.md` |
| Support Pack Wave | `.scratch/sceneforge-support-pack-wave/IMPLEMENTATION-PLAN.md`（Task 5–6 已勾） | `issues/07-support-llm-electron-acceptance.md` |
| Continue & Run | `.scratch/sceneforge-continue-and-run/IMPLEMENTATION-PLAN.md` | 设计文档 + ADR-0003 |
| Gate / Intake HITL | `.scratch/sceneforge-gate-intake-card-hitl/IMPLEMENTATION-PLAN.md` | Task 5 旧项目 |
| Flow Hardening | `.scratch/sceneforge-flow-hardening/progress.md` | Issue 07 含真 LLM |
| Engineering / Issue 20 | Phase 3 路线图 §5.1 | `.scratch/sceneforge-studio/issues/20-studio-stage-run-ux-hitl.md` |
| P5 UI 债 | — | `.handoff/handoff-20260619-p5-review-and-backlog.md` |

**Phase 3 后续包（需新 PRD）：** Style 深化、Validator 深化、Publish、全链、多轮 ACP — `docs/sceneforge/2026-06-18-sceneforge-phase3-roadmap.md` §2 之后。

---

## 6. 验证命令（接棒 / 提交前）

```bash
cd ~/Documents/trae_projects/lingji-creator
npx tsc --noEmit
npx vitest run tests/sceneforge-required-context.test.ts tests/sceneforge-semantic-validators.test.ts
npx vitest run tests/sceneforge-continue-run.test.ts tests/sceneforge-direct-llm-runner.test.ts
npx vitest run tests/sceneforge-support-llm-happy-path.test.ts tests/sceneforge-core-llm-happy-path.test.ts
npx vitest run tests/sceneforge-*.test.ts tests/sceneforge-*.test.tsx tests/electron-api.test.ts
npx vitest run tests/provider-migration.test.ts
git diff --check
```

人工（用户分阶段测时）：

1. 设置 → AI：默认 Provider + **准确模型 ID** + 测试连接成功。  
2. 新建项目 → intake/gate 手工/卡片 → reference 起 Direct LLM 链。  
3. 每阶段：无 required 警告 → Run → 提交草案 → Validate → Continue（可选 Continue & Run）。  
4. 记录失败阶段与 Alert 全文。

---

## 7. Git 提交建议（用户确认后）

1. `feat(sceneforge): gate/intake card HITL`（若本批含 UI）  
2. `feat(sceneforge): continue-run + script direct_llm`  
3. `feat(sceneforge): flow hardening required context + semantic validators`  
4. `feat(sceneforge): support pack context policy + tests`  
5. `fix(llm): default provider binding for sceneforge direct_llm`  
6. `chore(sceneforge): sync .scratch IMPLEMENTATION-PLAN`（可选）

---

## 8. 关键代码路径（LLM / 上下文）

| 能力 | 路径 |
| --- | --- |
| Direct LLM Runner | `electron/sceneforge/runners/scene-direct-llm-runner.ts` |
| Runner 工厂 | `electron/sceneforge/pipeline/scene-stage-runner-factory.ts` |
| 默认 LLM 解析 | `src/lib/llm/provider-utils.ts`（`resolveDefaultLlmBinding`、`formatLlmInvokeErrorMessage`） |
| `generateText` | `src/lib/llm/index.ts` `pickModel` |
| Headless 设置 | `electron/pipeline/headless-settings.ts` `loadFullHeadlessAISettings` |
| 阶段上下文 | `electron/sceneforge/pipeline/scene-context-builder.ts` |
| Required 阻塞 UI | `src/sceneforge/components/stage-run/StageRunPanel.tsx` |
| 能力表 | `src/sceneforge/lib/scene-stage-run-capabilities.ts` |
| Stage Pack | `prompts/sceneforge/stages/<stage>/` |

---

## 9. 权限与 Agent 环境（仍有效）

Claude **桌面端** 对 `src/`/`electron/` 可能 **EPERM**；`.handoff/`、`.scratch/` 通常可写。  
下一代理：先探针写小文件；若 EPERM → 建议用户 **Terminal `claude` 或新会话打开整仓**。  
勿默认让用户复制补丁进 `src/`（continue-run 补丁已过时，以仓库现状为准）。

---

## 10. 建议技能（下一代理）

| 场景 | 技能 |
| --- | --- |
| 补全 backlog 立项 | `brainstorming` → `writing-plans` → `planning-with-files` |
| 单阶段 bug（用户真机反馈） | `engineering/triage` 或 `engineering/diagnose` → `engineering/tdd` |
| LLM/Provider 改动 | `custom/karpathy-guidelines` |
| 大改后审查 | `custom/codereview` |
| E2E 清单 / 验收文档 | `custom/document-helper` |
| 编码前通读 | `custom/karpathy-guidelines`（全局规则） |
| 会话结束 | `handoff` |

---

## 11. 对用户的一句话

**Direct LLM 主链路在工程与用户侧已打通**；接下来按 §4 先 **补全后置能力与体验债**，你用 §6 +（待写的）E2E 清单 **按阶段压测**，代理再 **单点修细节**——不要在一轮里混做「新大包开发」和「全链真机签字」。

---

## 12. 历史交接（避免重复阅读）

- `.handoff/handoff-20260618-183200.md` — 旧阻塞（continue-run 补丁、权限）  
- `.handoff/handoff-20260619-{support-pack-wave,flow-hardening,p5-review}-*.md` — 各包收口  

本文取代上述文档中 **已过时** 的阻塞描述，并以 **LLM 已通 + 补全后置 + 分阶段测试** 为当前真相。
