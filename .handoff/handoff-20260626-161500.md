# Handoff - 2026-06-26 16:15:00

## 交接规则确认

本 handoff 承接：

```text
.handoff/handoff-20260626-154500.md
```

从本次开始，Phase 状态按以下规则判断：

```text
Phase 开发任务完成，只剩人工手动测试，也视为 Phase 完成。
每完成一个 Phase，都必须新建完整可追溯 handoff。
人工测试点集中记录在“待人工验证”中，后续由用户统一测试。
```

当前分支：

```text
sceneforge2.0-remix
```

当前阶段状态：

```text
Phase 0：完成 ✅
Phase 1：开发完成，待人工验证 ✅
Phase 2：开发完成，待人工验证 ✅
Phase 3：下一步开始
```

本轮没有运行本地命令：

```bash
npx tsc --noEmit
npx vitest run --reporter=basic
```

---

## 一、Phase 1：快速模式真实检测 + source_clip.mp4 最小闭环

### 目标

```text
fast PySceneDetect Worker
  → ShotDetectionResult
  → SourceSegment[]
  → SegmentClipService 生成真实 source_clip.mp4
  → write manifests/index
  → diagnostics 记录 detector/fallback/clipGeneration
```

### 开发完成内容

#### 1. Shot Detector 类型

```text
electron/sceneforge/remix/shot-detection/shot-detector-types.ts
commit: 3c2bd4ff86d75334e8e00ba6d9b350e3c9b533c5
```

#### 2. Python Worker Runner

```text
electron/sceneforge/remix/shot-detection/shot-detector-runner.ts
commit: 30b4d2bda41d16e6b634d6a9a222f48e501e7294
```

能力：解析 worker 脚本、解析 Python runtime、stdin JSON、stdout JSON、stderr 日志、超时保护、失败抛错供 fallback。

#### 3. Python fast detector

```text
resources/shot-detectors/common.py
resources/shot-detectors/fast_pyscenedetect.py
resources/shot-detectors/detect_shots.py
commits:
  4eb2a12a673b1f56e26145c47dad269afa48e8b9
  f2514b5eaf64d04b5b5bf82ea56e6b1e0b0b16a3
  7b41f3dc4eab2057adaf3fddfacc29a7e3a89326
```

fast 模式已接 PySceneDetect AdaptiveDetector。

#### 4. SegmentClipService

```text
electron/sceneforge/remix/segment-clips/segment-clip-service.ts
commit: 0f63ce917dde0a29b28e0f31b735ff68ec85f608
```

根据 `SourceSegment.timeRange` 生成真实 `source_clip.mp4`，默认 `reencode_accurate`。

#### 5. diagnostics 类型扩展

```text
src/sceneforge/remix/types/index.ts
commit: b5bcec591bee8dd8d312236ac7298ad58b52b78d
```

支持 detector、fallbackReason、detectionMetrics、clipGeneration。

#### 6. RemixSegmentationService 接线

```text
electron/sceneforge/remix/remix-segmentation-service.ts
commits:
  fdb4a96fbe735ac2ebb697733fcdf7945e41d06a
  c88cab78f55f2095a8c1b71f29f51870dfb0142e
```

当前行为：

```text
fast 模式优先调用 PySceneDetect
失败 fallback 规则候选
writeSegmentArtifacts 不再复制源视频，而是 ffmpeg 真实裁切 source_clip.mp4
```

### Phase 1 待人工验证

```text
1. fast 模式是否能跑 PySceneDetect。
2. PySceneDetect 不可用时是否 fallback 到 hybrid_fallback。
3. 每个 SourceSegment.sourceClipPath 是否真实生成 source_clip.mp4。
4. source_clip.mp4 是否为对应 timeRange 片段，不是整段源视频复制。
5. 有音频 / 无音频视频是否都能生成 clips。
6. 人工合并/拆分后 clips 是否同步重生成。
7. diagnostics.clipGeneration 是否显示生成数量和耗时。
```

---

## 二、Phase 2：TransNetV2 高精模式 + fallback

### 目标

```text
accurate 模式
  → 解析 TransNetV2 权重和 vendor 模型代码
  → 调用 PyTorch TransNetV2 worker
  → 输出 boundaries/scenes
  → 失败或缺资产时 fallback
```

### 开发完成内容

#### 1. accurate TransNetV2 worker

```text
resources/shot-detectors/accurate_transnetv2.py
commit: 1f7d14083322239ee29eb88590cc7ea1a564da14
```

能力：

```text
读取 modelPath / modelVendorDir
动态导入 vendor 模型结构代码
加载 .pth 权重
OpenCV 抽帧 resize 到 48x27 RGB
调用 TransNetV2 推理
predictions → boundaries/scenes
输出 ShotDetectionResult JSON
```

通用查找模块名：

```text
transnetv2_pytorch
transnetv2
model
models
net
nets
network
```

通用查找类名：

```text
TransNetV2
TransnetV2
TransNet
```

#### 2. detect_shots.py 接入 accurate

```text
resources/shot-detectors/detect_shots.py
commit: dd04fd27730b8c641f9dd3560e5ff3a7c801d1d4
```

当前：

```text
mode=fast     → fast_pyscenedetect.py
mode=accurate → accurate_transnetv2.py
```

#### 3. RemixSegmentationService 接入 accurate 资产解析和 fallback

```text
electron/sceneforge/remix/remix-segmentation-service.ts
commit: 2284163dbdbd5627fbf7b646aab649a55a0a3e42
```

当前 accurate 流程：

```text
resolveTransNetV2Assets()
  → 缺 weights/vendor：fallback 规则候选，diagnostics.fallbackReason 记录缺失项
  → assets ready：runShotDetector(mode=accurate, modelPath, modelVendorDir)
  → worker 成功：detector=transnetv2
  → worker 失败：fallback 规则候选，记录错误原因
```

#### 4. Shot Detectors README 更新

```text
resources/shot-detectors/README.md
commit: 9a906867c44e1bbeefd32d188541e18df720a889
```

补充：accurate 模式说明、vendor 查找规则、环境变量、手工 smoke test JSON 示例、常见失败检查项。

### Phase 2 待人工验证

```text
1. LINGJI_TRANSNETV2_MODEL_PATH 是否能正确解析。
2. LINGJI_TRANSNETV2_VENDOR_DIR 是否能正确解析。
3. Hugging Face 模型结构代码放入 vendor 后是否能被动态导入。
4. .pth 权重是否能 load_state_dict。
5. torch / opencv / numpy 环境是否满足 worker 运行。
6. accurate 模式是否输出 detector=transnetv2。
7. boundaries/scenes JSON 结构是否稳定。
8. 缺 weights/vendor 时是否 fallback 到 hybrid_fallback。
9. 权重或模型结构不匹配时 fallbackReason 是否清楚。
10. accurate 模式生成的 source_clip.mp4 是否仍按 segment timeRange 正确裁切。
```

---

## 三、当前风险

```text
1. 本地未跑 tsc，新增 import.meta.url / fileURLToPath 需编译验证。
2. 本地未跑 vitest，resolver 单测待验证。
3. Python worker 未做真实视频 smoke test。
4. TransNetV2 vendor 文件名和类名需按实际 HF 仓库确认。
5. .pth 与模型结构代码可能不匹配，需要人工 smoke test。
6. ffmpeg 裁切有音频/无音频、长短片段兼容性待测。
```

---

## 四、下一步：Phase 3

Phase 3 目标：片段导出与文件操作。

建议实现：

```text
sceneForgeRemix:exportSourceSegmentClips
sceneForgeRemix:regenerateSourceSegmentClips
sceneForgeRemix:openSourceSegmentClipFolder
```

前端入口：

```text
打开当前片段
在文件夹中显示
导出全部分镜片段
SegmentTable 显示片段状态
```

Phase 3 开发完成后，即使仍待人工测试，也要基于本 handoff 再新建下一份 handoff。
