# Handoff

## 本次会话焦点

继续做 `SceneForge` 阶段契约对齐，重点收口 `storyboard` 与 `video_prompts`，并把“后阶段产物优先级更高”的规则落到所有阶段的上下文提示词中。  
本次结束时，`video_prompts` 已完成代码层对齐与自动校验补强；人工联调**目前只推进到分镜（storyboard）阶段**，后续还需要继续从 `audio -> video_prompts -> export/publish` 往下测。

## 关键参考

- 对齐审计：`/Users/tangwujun/Documents/trae_projects/lingji-creator/docs/sceneforge/2026-06-19-sceneforge-stage-contract-alignment-audit.md`
- 实施计划：`/Users/tangwujun/Documents/trae_projects/lingji-creator/docs/superpowers/plans/2026-06-19-sceneforge-stage-contract-alignment.md`
- 执行跟踪：`/Users/tangwujun/Documents/trae_projects/lingji-creator/.planning/tasks/20260619-sceneforge-stage-contract-alignment/`
- 上一份用户指定 handoff：`/Users/tangwujun/Documents/trae_projects/lingji-creator/.handoff/handoff-20260619-115641.md`

## 本次实际改动

### 1. `storyboard` 对齐补强已完成

已与旧 `scene_forge` 的 `scene-storyboard-director` 规则对齐：

- 默认中文主导，英文仅保留必要术语。
- 强制 `storyboard_prompt_pack_plan` 写明总镜头数、单包/多包决策、每包覆盖范围。
- 默认规则：`total_shots <= 12` 倾向单包；`total_shots > 12` 倾向多包。
- `beat_skeleton / storyboard_content_breakdown / video_generation_units / shot_continuity_plan` 必须保持分段与包数一致。
- `control_board_prompts / style_board_prompts` 不再允许用松散英文摘要冒充正式交付。

生效文件：

- `prompts/sceneforge/stages/storyboard/system.md`
- `prompts/sceneforge/stages/storyboard/user.md`
- `prompts/sceneforge/stages/storyboard/review-checklist.md`
- `electron/sceneforge/validators/validators.storyboard.ts`

### 2. `video_prompts` 旧 skill 对齐已完成

这次最重要的收口点：

- 把 `video_prompts` 的默认主交付改成 **仅 `video_prompt_pack_cn` 必交**。
- 英文包改为可选：只在用户明确要求、平台需要英文或发布策略要求海外投放时才生成。
- `video_prompts` 正式 pack 默认中文主导，不允许无要求时整篇英文。
- `video_prompt_review` 已纳入正式 pack 必备 section。
- `video_prompt_pack_plan` 现在必须显式写出覆盖哪些 storyboard packs、哪些 segments，以及为什么这么分包。
- `【故事板关键帧参考规则】` 现在必须明确写出“控制故事板 Pack XX”为动作/连续性主参考，“风格故事板 Pack XX”为渲染/氛围辅助参考。
- validator 新增：
  - 中文主导校验
  - pack coverage 校验
  - storyboard pack reference 校验
  - VGU trace 校验
  - 与 storyboard 的 Segment / VGU 对齐校验（在上游文本可解析时生效）

生效文件：

- `prompts/sceneforge/stages/video_prompts/system.md`
- `prompts/sceneforge/stages/video_prompts/user.md`
- `prompts/sceneforge/stages/video_prompts/review-checklist.md`
- `prompts/sceneforge/stages/video_prompts/context-policy.yaml`
- `prompts/sceneforge/stages/video_prompts/output-contract.yaml`
- `prompts/sceneforge/stages/video_prompts/handoff-template.yaml`
- `electron/sceneforge/validators/validators.video-prompts.ts`
- `electron/sceneforge/pipeline/scene-stage-definitions.ts`
- `electron/sceneforge/service.ts`
- `electron/sceneforge/export/scene-prompt-pack-exporter.ts`

### 3. `video_prompts` 上游依赖也同步收紧

当前 `video_prompts` 阶段除了 `storyboard.storyboard_prompt_pack` 外，`storyboard.control_board_prompts` 和 `storyboard.style_board_prompts` 也已改成 required input，原因是下游 copy-ready blocks 明确引用它们，继续保留 optional 会导致 pack 继承关系失真。

## 已完成验证

已跑回归：

```bash
npx vitest run tests/sceneforge-validator.test.ts tests/sceneforge-context-policy.test.ts tests/sceneforge-stage-pack.test.ts tests/sceneforge-stage-context.test.ts tests/sceneforge-ui.test.tsx tests/sceneforge-core-llm-contract.test.ts tests/sceneforge-direct-llm-runner.test.ts tests/sceneforge-core-llm-happy-path.test.ts tests/sceneforge-export.test.ts tests/sceneforge-core-mvp-placeholder.test.ts
```

结果：`10` 个测试文件，`94` 条测试全部通过。

另外，这次把一批旧的测试夹具也补成了符合当前 validator 的正式样例，避免后续回归被早期占位文本误伤。

## 当前人工测试状态

- 用户已经人工看过并反馈过 `design / storyboard` 阶段的若干问题。
- 目前人工测试**只到分镜阶段**。
- `video_prompts` 虽然代码契约、自动校验和测试已通过，但**还没有完成真实工作流下的人工联调验收**。
- `audio -> video_prompts -> export/publish` 这条链路，仍需要下一会话继续手工跑。

## 下一会话建议顺序

1. 在 Studio 里从一个真实项目继续，确认 `storyboard` 已 approved。
2. 人工跑 `audio`，检查：
   - 是否正确继承 `design.design_prompts`
   - 是否保留 `BGM / Foley-SFX / Ambience / Silence`
3. 人工跑 `video_prompts`，重点检查：
   - 默认是否只生成 `video_prompt_pack_cn`
   - 是否不再默认生成全英文正文
   - `video_prompt_pack_plan` 是否写明 storyboard pack / segment 覆盖关系
   - `【故事板关键帧参考规则】` 是否真的引用 `控制故事板 Pack XX / 风格故事板 Pack XX`
   - `VGU / continuity / timecode` 是否和 storyboard 对得上
4. 如果人工联调通过，再继续看 `export` / `publish` 的展示与导出是否仍符合预期。

## 风险与提醒

- 当前仓库 worktree 很脏，存在大量与本次任务无关的改动；下一位代理不要做全局清理，也不要误回滚用户已有更改。
- `video_prompt_pack` 旧 key 仍保留为兼容提交 key，但正式 output contract 已切到 `video_prompt_pack_cn` 为唯一必交。后续如果看到旧 key，不要再把它恢复成默认主契约。
- `video_prompts` 的中文主导校验目前是启发式阈值，不是语义级判定；如果后续真实产物仍被误判，可继续微调 `validators.video-prompts.ts` 的 `hasChineseLedBody()`。

## 建议技能

- `karpathy-guidelines`
- `handoff`
- `document-helper`
- `codereview`

## 下一次会话建议关注点

继续做 `audio -> video_prompts -> export/publish` 的真实项目人工联调，确认 `video_prompts` 的中文主交付、分包继承、VGU/Segment 对齐和导出展示都在 Studio 里实际生效。
