# Lingji Creator SceneForge Handoff - 2026-06-17 16:40:00

## 当前状态

仓库：`/Users/tangwujun/Documents/trae_projects/lingji-creator`  
分支：`codex/sceneforge-studio-core`

**Phase 1（Issues 01–12）**：已提交（commit `8c460d2` 等）。  
**Phase 2（Issues 13–21）**：本地已实现并有多笔 commit；**Issue 20 需你在 Electron 内点跑后反馈**。

## 本会话 / 延续工作完成项

| Issue | 交付摘要 |
|-------|----------|
| 13 | `scene-context-policy.ts` + core 三阶段 `context-policy.yaml` |
| 14 | `SceneContextBuilder`，废除 9 全文 `getStageContext` |
| 15 | `SceneHandoffWriter` + P0 `handoff-template.yaml` |
| 16 | `scene_get_stage_context` / IPC 扩展 `runner`、`selectedAssetIds` |
| 17 | P0 stage pack 文件 + `loadSceneStagePack` auxiliaryPaths |
| 18 | `scene-prompt-renderer` + `scene-direct-llm-runner` |
| 19 | `sceneRunStage` 三件套 + MCP `scene_run_stage` + acp MVP |
| 20 | `StageRunPanel` + `task-progress`（ai-write） |
| 21 | `sceneforge-phase2-integration.test.ts` + 文档/PRD 更新 |

## 关键 Git 提交（Phase 2，从新到旧摘录）

- `3e66727` — Issue 20 Studio run panel
- `a5873ad` — Issues 17–19 packs + runners
- `f7e2db0` — Issue 16 IPC/MCP runner params
- `68fd92e` — Issue 15 handoff
- `f69e741` — Issue 14 builder
- `5da1982` — Issue 13 context policy

## 验证（上次全绿）

```bash
npx vitest run tests/sceneforge-*.test.ts tests/electron-api.test.ts
npx tsc --noEmit
```

含 **`tests/sceneforge-phase2-integration.test.ts`**：handoff、video+audio/performance 上下文、direct_llm mock → submit。

## 三种 Runner（行为）

| Runner | 说明 |
|--------|------|
| `manual_submit` | 返回 `manualArtifacts`，不写盘 |
| `direct_llm` | 需应用 `settings.json` LLM；返回 JSON 草案 map |
| `acp_agent` | 无 Agent 配置 → 友好错误；有配置 → `__acp_session_brief`（完整 ACP 循环未接） |

统一出口仍为：`submitStageDraft` → Validator → Approval → Handoff。

## 请你本地反馈（Issue 20 HITL）

1. 打开 SceneForge 项目 → Studio  
2. 选 core 阶段 → 执行方式 → **运行本阶段**（可先 `manual_submit` 或已配 LLM 的 `direct_llm`）  
3. 看底部统一进度条；再试 Validate / Approve / Copy  
4. 把异常现象、期望行为发回即可

## 未完成 / P2（见 D4）

- 支撑阶段 Pipeline UI、script pack、ACP 真会话、style 选择器 IPC UI  
- Validator 扩展 audio/performance 语义层  
- `pipeline.yaml` 项目级、ZIP 导出、自动推进链  

文档：[`docs/sceneforge/2026-06-17-sceneforge-phase2-overview.md`](docs/sceneforge/2026-06-17-sceneforge-phase2-overview.md)

## 注意事项

- 勿 revert 未提交/已提交的 SceneForge 工作除非有意回滚。  
- IPC 改动须 main / preload / `electron-api` 同步。  
- `.scratch` / `.planning` 仍多为本地 issue 追踪，未全部入 git。

## 建议技能（后续修 bug / 迭代）

- `karpathy-guidelines` / `codereview`  
- `verify`：Electron 点验 Issue 20  
- `handoff`：阶段结束再更新 `.handoff/`