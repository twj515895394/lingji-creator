# Lingji Creator SceneForge Handoff - 2026-06-17 19:51

## 当前状态

| 项 | 值 |
| --- | --- |
| 仓库 | `/Users/tangwujun/Documents/trae_projects/lingji-creator` |
| 分支 | `codex/sceneforge-studio-core` |
| Phase 2（Issues 13–21） | 已提交（context、pack、runner、StageRunPanel、集成测试） |
| **P0 工坊包（`.scratch/sceneforge-studio-p0/`）** | **Issues 01–12 本地实现完成**（本会话收尾 Issue 12 回归 + 本文） |

上轮交接：[`handoff-20260617-171500.md`](handoff-20260617-171500.md)（问题分析 + P0 排期）

---

## 1. P0 已完成项（对照 PRD，诚实边界）

### 1.1 文案与产品名

- [x] 用户可见主名：**视频内容创作工坊**（`data-testid="sceneforge-product-eyebrow"`）
- [x] 副文案：从选题与素材到设定、分镜与视频模型提示词
- [x] 创建向导：`SceneForgeCreateDialog` 双入口（`source_intake` / `topic_gate`）
- [x] `project.json.sceneforge.entryPath` 持久化；`sceneGetProjectState` 返回 `entryPath`

### 1.2 13 阶段流水线侧栏

- [x] `src/sceneforge/lib/scene-pipeline-ui.ts` + Vitest
- [x] 分组：前期 / 制作 / 交付；全阶段可点选
- [x] 就绪标签：工坊 / Agent / P0（`scene-stage-capabilities.ts`）

### 1.3 Studio Shell 与 UI（Issue 06–07）

- [x] 贴边三栏：`separator` 1px grid，无全页 24px 卡片式 padding
- [x] 列头 40px、`--color-panel-bg`
- [x] 主 CTA / 检查器：`Button`、`Tabs`、`Alert`（`src/ui`）
- [x] `StageRunPanel`、`ArtifactCopyPanel` 复制/运行按钮迁移

### 1.4 工作区与 HITL 最小路径（Issue 08–11）

- [x] **source_intake / topic_gate**：`SceneSupportStageWorkspace` + `submitStageDraft`（ADR-0001）
- [x] 校验器：`validators.source-intake.ts`、`validators.topic-gate.ts`
- [x] MCP：`scene_submit_stage_draft` / `validate` / `approve` / `run_stage` 含 intake/gate
- [x] **支撑阶段**：`SceneSupportPlaceholderWorkspace`（MCP 指引）
- [x] **export**：`SceneExportWorkspace` → `sceneExportPromptPack`

### 1.5 文档与纪律

- [x] `CLAUDE.md` SceneForge 节（产品名、UI 对齐、P0 路径）
- [x] `docs/adr/0001-sceneforge-entry-path-and-studio-hitl.md`
- [x] 设计包：`.scratch/sceneforge-studio-p0/`（PRD、DESIGN、issues 01–12）

---

## 2. 仍未完成（PRD「超出范围」或未做深）

| 项 | 说明 |
| --- | --- |
| intake **改编方向卡片**（5–10 选 1 闸门） | **部分完成**：`## 改编方向` 解析 + 卡片确认 → `adaptation_selection`；校验 gate `SCENE_INTAKE_ADAPTATION_PENDING` |
| topic_gate **风格确认卡片** / go·observe·drop 可视化 | **部分完成**：决策 + 风格卡片 → `gate_confirmations`；侧栏 reference+ 阻塞文案 |
| 支撑阶段完整 pack + Validator 全链 | 仅 core + intake/gate submit |
| 真 ACP 多轮、`scene_start_stage` 自动链 | Phase 2 acp MVP = briefing |
| script/performance/reference **Studio 表单** | 占位 + Agent |
| ZIP 导出、publish 完整 UI | export 仅 Prompt Pack |
| 侧栏 entryPath **阻塞/折叠** 联动（DESIGN §1.3） | **部分完成**：`getStageNavBlockReason` + 侧栏 hint；topic_gate 风格未确认阻塞 reference 及之后 |
| `SceneForgeStudioShell` 独立文件拆分 | 逻辑仍在单页，Shell CSS 已对齐 |

**勿误标为已完成：** 全旧 SOP 产品化、全支撑链 Studio 可点跑。

---

## 3. 验证结果（Issue 12，2026-06-17）

```bash
npx vitest run tests/sceneforge-*.test.ts tests/electron-api.test.ts   # 105 + 5 passed（含 HITL）
npx tsc --noEmit                                                        # 通过
```

新增/相关测试文件（节选）：

- `sceneforge-pipeline-ui.test.ts`
- `sceneforge-entry-path.test.ts` / `sceneforge-create-entry-path.test.ts`
- `sceneforge-support-submit.test.ts`
- `sceneforge-workspace-routing.test.ts`
- `sceneforge-phase2-integration.test.ts`（未改坏）

---

## 4. 人工验收步骤（建议维护者）

### 4.1 两种 entryPath

1. 欢迎/工具栏 → 新建 SceneForge 项目 → 选 **从视频/链接解析** → 创建目录。
2. 进入工坊 → 侧栏默认落在 **源视频解析**；提交 `source_material` 草案 → Validate → 产物出现在检查器。
3. 新建第二项目 → 选 **从选题与想法** → 默认 **选题闸门**；提交 `topic_brief`。

### 4.2 13 阶段与 Shell

1. 侧栏数全阶段（前期含 intake/gate/reference/story/assets；制作含 design…audio；交付含 video_prompts/publish/export）。
2. 全屏三栏贴边，无大块圆角外框；与 Cut Editor 对比字号/列头。
3. 选 **故事方向** → 中栏 MCP 占位（`data-testid="scene-support-placeholder"`）。
4. 选 **导出** → **导出 Prompt Pack** 成功显示路径。

### 4.3 Core 回归（Phase 2）

1. 打开已有 core 数据项目 → design → **运行本阶段** / Validate / Approve。
2. 右栏 Tabs：Preview / Copy / Raw。

### 4.4 UI 对齐清单（快速）

- [ ] 顶栏 eyebrow + 标题 token 字阶
- [ ] 三栏 separator 分隔
- [ ] Validate / Approve / 运行本阶段 为统一 Button 样式
- [ ] 检查器 Tabs 为 ui TabsList

---

## 5. 重要文件索引

| 用途 | 路径 |
| --- | --- |
| Studio 页 | `src/sceneforge/pages/SceneForgeStudio.tsx` + `.module.css` |
| 创建入口 | `src/sceneforge/components/SceneForgeCreateDialog.tsx` |
| 工作区 | `src/sceneforge/components/workspace/*` |
| 流水线 UI 模型 | `src/sceneforge/lib/scene-pipeline-ui.ts` |
| 服务 / submit | `electron/sceneforge/service.ts` |
| P0 PRD | `.scratch/sceneforge-studio-p0/PRD.md` |
| Phase 2 总览 | `docs/sceneforge/2026-06-17-sceneforge-phase2-overview.md` |

---

## 6. 建议下一批

1. **HITL 深化**：intake 改编方向列表 + topic_gate 风格确认（仍走 `submitStageDraft`）。
2. **侧栏阻塞**：`entryPath` + 依赖未满足时展示 DESIGN 阻塞文案。
3. **Issue 20 记录**：Electron 点跑 core + intake/gate 结果写入 handoff 或 issue。
4. **提交**：用户确认后 `git commit`（当前为工作区改动，未在本 handoff 代提交）。

---

## 7. 注意事项

- IPC 三件套：`electron/main.ts`、`preload.ts`、`src/lib/electron-api.ts`
- SceneForge UI 禁止新增硬编码字阶（见 `CLAUDE.md`）
- `.scratch/` 设计包未必全部入 git，以仓库实际 tracked 文件为准