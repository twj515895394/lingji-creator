# Handoff - 2026-06-26 17:05:00

承接：`.handoff/handoff-20260626-163500.md`

当前状态：

```text
Phase 0：完成
Phase 1：开发完成，待人工验证
Phase 2：开发完成，待人工验证
Phase 3：开发完成，待人工验证
```

本次没有运行本地 `tsc` / `vitest` / 人工视频测试。

---

## Phase 3 目标

```text
片段导出与文件操作
  → 生成项目内分镜片段包
  → 重新生成 source_clip.mp4
  → 返回片段文件夹/片段路径
  → IPC / preload / UI 挂载
```

---

## 已完成内容

### 1. 后端片段包与维护能力

```text
electron/sceneforge/remix/segment-clips/segment-clip-exporter.ts
electron/sceneforge/remix/segment-clips/segment-clip-maintenance.ts
```

能力：

```text
exportSourceSegmentClips
  → 生成项目内部 exported_clips 片段包
  → 每个导出片段重新按 timeRange 生成
  → 写 clips_manifest.json

regenerateSourceSegmentClips
  → 重新生成 source_segments/{segmentId}/source_clip.mp4
  → 更新 diagnostics.clipGeneration

resolveSourceSegmentClipFolder
  → 返回当前片段 source_clip.mp4 路径与所在目录
  → 不直接调用 shell.openPath，避免主进程副作用
```

导出目录：

```text
sceneforge/remix/source-assets/{sourceAssetId}/exported_clips/
```

### 2. 独立 IPC 模块

```text
electron/sceneforge/remix/segment-clips/segment-clip-ipc.ts
commits:
  a6181131f0c959f7199b7e1c50ca6aaf33ae352f
  8ff688db89c458a04f3b7563759e23886e2fa7f0
```

暴露：

```text
sceneForgeRemix:exportSourceSegmentClips
sceneForgeRemix:regenerateSourceSegmentClips
sceneForgeRemix:openSourceSegmentClipFolder
```

### 3. Remix IPC 注册

```text
electron/sceneforge/remix/remix-ipc.ts
commit: f80dfb61b3870a0338a47ad48e8e5be182052c06
```

在 `registerSceneForgeRemixIpc()` 开头注册：

```ts
registerSceneForgeRemixSegmentClipIpc();
```

### 4. preload 独立桥接

```text
electron/sceneforge/remix/segment-clips/segment-clip-preload.ts
commit: 49382fe4458352168517e164018525d5cb167891
```

暴露到 renderer：

```text
window.sceneForgeRemixSegmentClips.exportSourceSegmentClips
window.sceneForgeRemixSegmentClips.regenerateSourceSegmentClips
window.sceneForgeRemixSegmentClips.openSourceSegmentClipFolder
```

### 5. 构建期注入 preload bridge

```text
electron.vite.config.ts
commit: 0e08537542c90f521b7b6cee936825c115cf31eb
```

通过 preload transform 在 `electron/preload.ts` 构建时追加：

```ts
import './sceneforge/remix/segment-clips/segment-clip-preload';
```

避免直接完整替换 700+ 行 `preload.ts`。

### 6. renderer 类型声明

```text
src/sceneforge/remix/segment-clip-api.d.ts
commit: 2986a288c1d43e6122ec289f478c08e1ea662e60
```

为 `window.sceneForgeRemixSegmentClips` 增加全局类型。

### 7. 前端操作面板

```text
src/sceneforge/remix/components/SegmentClipActionsPanel.tsx
commit: f818f025930348bd1e0577e405493d9149923b93
```

按钮：

```text
导出全部片段
重新生成片段
打开当前片段
```

### 8. 构建期注入前端操作面板

```text
electron.vite.config.ts
commit: 326353c2959d9368ce492b87ec265b5893a37d1b
```

通过 renderer transform 给 `src/sceneforge/remix/pages/RemixAssetProcessing.tsx` 注入：

```text
SegmentClipActionsPanel
```

避免直接完整替换大型页面文件。

---

## 特别记录：main.ts 截断风险已处理

曾尝试直接更新 `electron/main.ts` 注册独立 IPC 模块，提交：

```text
ce869b4a0c313b5eddb7bc0a867c93bef2154df3
```

该提交导致 `main.ts` 被截断。已立即将分支指针回退到安全提交：

```text
8ff688db89c458a04f3b7563759e23886e2fa7f0
```

随后改为在 `remix-ipc.ts` 内注册独立模块，避免继续改 `main.ts`。

---

## Phase 3 待人工验证

```text
1. 构建时 preload transform 是否成功注入 segment-clip-preload。
2. renderer 中 window.sceneForgeRemixSegmentClips 是否存在。
3. RemixAssetProcessing 页面是否显示 SegmentClipActionsPanel。
4. 点击“导出全部片段”是否生成 exported_clips 目录和 clips_manifest.json。
5. 导出片段是否按当前 segment timeRange 生成。
6. 点击“重新生成片段”是否重建 source_segments/{segmentId}/source_clip.mp4。
7. 点击“打开当前片段”是否通过 showItemInFolder 打开正确文件位置。
8. 无片段时导出是否给出清晰错误。
9. ffmpeg 失败时错误是否能展示到面板 message。
```

---

## 下一步

可以进入 Phase 4：真实关键帧抽取。

Phase 4 目标：

```text
remix-keyframe-service.ts
  → 不再写 1px 占位 PNG
  → 使用 ffmpeg 从 source video / source_clip.mp4 抽取 first/middle/last
  → 写 first_frame.png / middle_frame.png / last_frame.png
  → 失败时记录明确错误
```
