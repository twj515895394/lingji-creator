# Lingji Creator SceneForge Handoff - 2026-06-17 22:13

## 当前状态

| 项 | 值 |
| --- | --- |
| 仓库 | `/Users/tangwujun/Documents/trae_projects/lingji-creator` |
| 分支 | `codex/sceneforge-studio-core` |
| 工作区 | **大量未提交改动**（Studio P0 + HITL + 布局 + 流程按钮修复） |
| 上轮交接 | [handoff-20260617-195100.md](handoff-20260617-195100.md) |

---

## 1. 本会话完成项（相对 195100）

### 1.1 产品与流程（用户反馈驱动）

| 主题 | 交付 |
| --- | --- |
| 选题/源材料 UI | 单行输入 + gate 总时长/分段芯片；`SceneGateBriefForm` / `SceneIntakeBriefForm`；成功/失败 `Alert` |
| 子标题字阶 | 字段标签加粗主色，说明正文灰色（`SceneStageBriefForms` / `SceneGateConfirmPanel`） |
| 三栏 Shell | 页面外边距 + 圆角外框；复用 **`ResizeHandle`** + `useSceneForgeStudioLayout`（localStorage 宽度） |
| 侧栏 | 字号调大（`lg`）、默认宽 272px；阻塞说明 **同行小字**（`shortenStageHint`） |
| Gate 决策 UI | 三选项选中高亮；去掉「前往参考分析」 |
| **Validate → Continue 流程** | `SceneStageFlowActions`：通过后绿色「已通过」；**Continue** 审批并 **`getNextPipelineStage` 自动切阶段** |
| **Continue 灰掉 bug** | `topic_gate` 默认策略 `optional` → 状态 `validated` 非 `waiting_approval`；已扩展 `canContinueStage` + `approveSceneStage` 接受 `validated` |
| **运行时错误** | 移除残留 `canApprove` 引用（`ReferenceError` 已修） |

### 1.2 HITL（部分）

- `adaptation_selection` / `gate_confirmations` 侧车产物 + 校验 gate
- `SceneGatePostConfirm` 摘要（无长文 AI 说明）
- 侧栏 `getStageNavBlockReason`（风格未确认阻塞 reference+）

### 1.3 文档 / 追踪

- P0 issues 01–12 在 `.scratch/.../issues/` 标 **completed**（本地）
- Phase2 overview 指针已更新（195100）

---

## 2. 用户诉求与产品理解（需下一会话记住）

1. **工坊 = 视频内容创作流水线**，不是「Prompt Pack 工具」；完整 **13 阶段** 可见、可测。
2. **确认风格 ≠ 自动 AI 生成**；用户曾期望「继续推送」——当前约定是：**Validate → Continue（审批+切阶段）**，下一阶段再 **运行本阶段 / Agent**。
3. **Continue 流程**（用户确认的理解，已实现逻辑，待 HITL 再验）：
   - 确认风格后 → Validate 可点 → 通过变绿 → Continue 可点 → 自动进入下一阶段（如 reference）。
4. **UI**：三列可拖拽、侧栏勿易滚动、中栏分段清晰（`InspectorSection`）、主按钮勿全宽。

---

## 3. 验证命令（改代码后必跑）

```bash
npx vitest run tests/sceneforge-*.test.ts tests/electron-api.test.ts
npx tsc --noEmit
```

**注意**：`approveSceneStage` 对 `validated` 的改动在 **electron 主进程**；需 **重启 dev / 重编 main** 后 Electron 内 Continue 才完整生效。

本会话末自动化（未在本轮 handoff 前全量重跑）：曾有多轮 `tsc` 通过、`sceneforge-*` 测试通过；`canApprove` 修复后建议再跑一遍。

---

## 4. 未完成 / 待优化（优先级）

### P0 残留 / 体验

| 项 | 说明 |
| --- | --- |
| **人工验收 Issue 20** | Electron 内完整走通：双 entryPath → gate 确认 → Validate → Continue → reference；**记录结果到 handoff** |
| **git commit** | 当前分支工作区大量 `M`/`??`，用户未要求提交；合并前需整理 commit |
| **侧栏 localStorage 旧宽度** | 用户若仍觉窄，清 `sceneforge-studio-sidebar-width-v1` 或拖宽 |
| **core 阶段 Continue** | 已接 `SceneStageFlowActions`，与 support 的 `flowCanContinue` 逻辑需与 optional/required 策略再验一遍 |
| **已 approved 再点 Continue** | 仅跳下一阶段，不调 approve（已实现，需测） |

### P1 产品 / 功能

| 项 | 说明 |
| --- | --- |
| intake/gate **卡片式 HITL 深化** | 改编方向、风格确认已有 UI；Agent 自动填方向列表、topic_gate 评分展示等 |
| **确认后可选自动 `sceneRunStage(reference)`** | 用户若坚持「Continue=开始生成」，需产品确认 + 任务进度 |
| 支撑链 **Studio 表单** + 全 pack / Validator |
| 侧栏 **entryPath 折叠/跳过** 文案（DESIGN §1.3 完整版） |
| `SceneForgeStudio` **拆 Shell 文件**（结构债） |
| Setup/欢迎 **文案** 全量扫「Prompt Pack」 |

### P2+（PRD 超出范围）

真 ACP 多轮、一键全链、ZIP 导出、publish 完整 UI、script 等完整 Studio。

---

## 5. 关键路径索引

| 用途 | 路径 |
| --- | --- |
| Studio 页 | `src/sceneforge/pages/SceneForgeStudio.tsx` + `.module.css` |
| 布局拖拽 | `src/sceneforge/hooks/useSceneForgeStudioLayout.ts`，`src/components/ResizeHandle.tsx` |
| Validate/Continue UI | `src/sceneforge/components/workspace/SceneStageFlowActions.tsx` |
| Gate 表单/确认 | `SceneGateBriefForm.tsx`，`SceneGateConfirmPanel.tsx`，`SceneGatePostConfirm.tsx` |
| 下一阶段 | `src/sceneforge/lib/scene-stage-nav.ts` |
| 审批状态机 | `electron/sceneforge/pipeline/scene-state-machine.ts`（`approve` 含 `validated`） |
| ADR 入口/HITL | `docs/adr/0001-sceneforge-entry-path-and-studio-hitl.md` |
| P0 包 | `.scratch/sceneforge-studio-p0/`（PRD、DESIGN、issues） |
| Phase 2 | `docs/sceneforge/2026-06-17-sceneforge-phase2-overview.md` |

---

## 6. 已知问题记录（本会话）

| 问题 | 状态 |
| --- | --- |
| Continue 在 Validate 通过后仍灰 | **已修**（optional → `validated`） |
| `canApprove is not defined` | **已修** |
| 确认风格后界面像「断档」 | **已改**为流程条 + 自动切阶段；**非**自动 AI |
| 侧栏/中栏/UI 丑、无拖拽 | **已改**一轮；用户可继续提审美细节 |
| 子标题与正文同色 | **已改**字阶 |

---

## 7. 建议下一批工作（维护者）

1. **重启 Electron** → 走通 topic_gate **Continue → 参考分析** → 记录 Issue 20。
2. 满意后 **git commit**（建议拆分：P0 shell / HITL / flow-actions / state-machine）。
3. 若要做「Continue 后自动跑 reference Runner」：先 **brainstorming** 再小步 IPC。

---

## 8. 建议技能（下一会话）

| 场景 | 技能 |
| --- | --- |
| 继续 Studio / gate 流程 | `brainstorming` → `writing-plans` → `tdd` |
| UI 再抛光 | `design-taste-frontend`（项目内 `/design-taste-frontend`） |
| 主进程/state 机 | `karpathy-guidelines`、`diagnose` |
| 提交前审查 | `codereview` |
| 文档/ADR 更新 | `document-helper` |
| 阶段结束 | `handoff` |

---

## 9. 注意事项

- IPC 三件套：`electron/main.ts`、`preload.ts`、`src/lib/electron-api.ts`
- SceneForge UI：勿硬编码字阶（`CLAUDE.md`）
- `.scratch/`、`.handoff/` 未必全部入 git