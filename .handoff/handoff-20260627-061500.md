# Handoff — Remix 原片理解 Issue 06/07 实现收口

> 接替：[`handoff-20260626-215005.md`](handoff-20260626-215005.md)（设计 backlog）· 代码基线 commit `04da374`（Issue 05）  
> 日期：2026-06-27  
> 状态：**Issue 06 + 07 已在工作区实现，未 git commit**

---

## 1. 本轮已完成（代码）

| Issue | 状态 | 摘要 |
|-------|------|------|
| **06** 理解工作台与标注预填 | **done（未提交）** | `UnderstandingWorkbenchPanel`、workbench loader、IPC `getSourceUnderstandingWorkbench` / `rerunSegmentUnderstanding`、标注 AI 预填 |
| **07** 一键编排 | **done（未提交）** | `RemixUnderstandingOrchestrator` + `runSourceUnderstanding` 单 job；`runWithProgress` 并发默认 2；台词 fresh 跳过 whisper |

### 1.1 关键文件

| 区域 | 路径 |
|------|------|
| 编排器 | [`electron/sceneforge/remix/remix-understanding-orchestrator.ts`](../electron/sceneforge/remix/remix-understanding-orchestrator.ts) |
| 工作台聚合 | [`electron/sceneforge/remix/remix-understanding-workbench.ts`](../electron/sceneforge/remix/remix-understanding-workbench.ts) |
| 服务入口 | [`electron/sceneforge/remix/remix-service.ts`](../electron/sceneforge/remix/remix-service.ts)（`runSourceUnderstanding` / `patchUnderstandingJob`） |
| 片段 LLM 并发 | [`electron/sceneforge/remix/remix-understanding-service.ts`](../electron/sceneforge/remix/remix-understanding-service.ts)（`runWithProgress`） |
| UI | [`src/sceneforge/remix/pages/RemixAssetProcessing.tsx`](../src/sceneforge/remix/pages/RemixAssetProcessing.tsx)、[`UnderstandingWorkbenchPanel.tsx`](../src/sceneforge/remix/components/UnderstandingWorkbenchPanel.tsx) |
| Job 类型 | [`src/sceneforge/remix/types/index.ts`](../src/sceneforge/remix/types/index.ts)（`RemixUnderstandingJobProgress`） |

### 1.2 行为说明（权威）

- **一次点击「生成原片理解」**：`runSourceUnderstanding` → 编排器顺序：缺音频则抽音频 →（可选跳过）全片 whisper+对齐 → 片段理解（并发 2）→ rollup（在 understanding service 内）。
- **跳过台词**：`isSourceTranscriptFresh` 比对 `source_transcript.json` 的 `inputHash.audioSha256` 与当前 `source_audio` 文件 hash。
- **单段重跑**：`rerunSegmentUnderstanding` 仍只跑 LLM 单段（不经编排器），符合专篇 §3 补救路径。
- **标注预填**：仅当无已保存 tags/note/`lastAnnotatedAt` 时应用 `annotationPrefill`；有保存内容不覆盖。

### 1.3 本地 STT 模型

- 本机已放置：`tools/local-stt/whisper/ggml-small.bin`（约 465MB，**当前未跟踪**）。
- **提交时勿纳入该 bin**；保持 README 指引 + 本机路径即可。

---

## 2. 验证记录

```bash
npx tsc --noEmit                    # 通过
npx vitest run \
  tests/sceneforge-remix-understanding-workbench.test.ts \
  tests/sceneforge-remix-understanding-orchestrator.test.ts \
  tests/sceneforge-remix-understanding.test.ts \
  tests/sceneforge-remix-asset-processing.test.tsx
# 13 passed
```

未做：`./dev.sh` 实机 E2E、26 段进度长任务手测。

---

## 3. Review 摘要（全方面，2026-06-27）

### 3.1 应修（建议下一迭代）

| 级别 | 项 | 说明 |
|------|-----|------|
| **P2** | 进行中 job 进度 UI | `runAction` 期间 `activeJob` 为本地占位，**未轮询** `processingJobs`；编排器写入的 `understandingProgress` 在长任务运行中可能**不实时**显示。对齐 ui-recovery **#16** 或理解步骤内轮询 `getSourceAsset`。 |
| **P2** | 大文件误提交 | `tools/local-stt/whisper/ggml-small.bin` 应在 `.gitignore` 明确忽略（若尚未）。 |
| **P3** | 并发可配置性 | `understandingConcurrency` 仅 `RemixServiceOptions` 构造参数，**未**接应用设置 / `remix.understanding.concurrency`。 |
| **P3** | Issue 07 验收缺口 | 单测仅覆盖「跳过 whisper」；**未**覆盖 N 段 `completed/total` 递增（issue 建议 26 段 mock）。 |
| **P3** | 进度文案 | 编排器 `onProgress` 顶栏 message 用 `completed+1` vs `completed` 略不一致。 |

### 3.2 无阻塞 / 符合设计

- 门禁：理解完成后仍为 `ready_for_review` + 真实 JSON，非占位 approved（延续 Issue 01/05）。
- `rerunSegmentUnderstanding` 不重复跑 transcript：正确。
- Mock API 已补 `getSourceUnderstandingWorkbench` / `rerunSegmentUnderstanding` / audio+transcript 桩。
- `getProcessingStepLabel` 已支持 `remix_audio_extraction` / `remix_transcript` job step。

### 3.3 残留文档债（非本轮回滚）

- `handoff-20260626-215005` backlog 表仍写 06/07 **pending** — 应以本 handoff 为准。
- `docs/sceneforge-remix/*`、`.scratch/sceneforge-remix-source-understanding/*` 仍为未提交文档包。

---

## 4. 建议下一 agent 动作

1. **（可选）** 修 P2：理解步骤 running 时轮询 job 或合并 `activeJob` 与 snapshot `activeProcessingJob.understandingProgress`。
2. **实机**：`./dev.sh` → 关键帧完成 → 一次「生成原片理解」→ 看 workbench 卡片 + 磁盘 `segment_understanding.json` / `source_overview.json`。
3. **提交范围**（建议拆分）：
   - `feat(remix): understanding workbench and annotation prefill (issue 06)`
   - `feat(remix): source understanding orchestrator (issue 07)`
   - 文档/scratch 是否单独 commit 由用户定。
4. **不要 commit**：`ggml-small.bin`。

---

## 5. 技能

| 场景 | 技能 |
|------|------|
| 修进度轮询 / E2E 失败 | [`diagnosing-bugs`](/Users/tangwujun/.agents/skills/engineering/diagnosing-bugs/SKILL.md) |
| 提交前审查 | [`codereview`](/Users/tangwujun/.agents/skills/engineering/codereview/SKILL.md) |
| 实机慢 | 读 [`AGENTS.md`](/Users/tangwujun/Documents/trae_projects/lingji-creator/AGENTS.md) auto-run jsonl 流程（Remix 若未接 telemetry 则先补） |

---

## 6. Issue 01–05 回顾审查（2026-06-27）

> 基线：git `04da374` 及之后工作区；以下对照各 issue **验收标准** 与现有 vitest。

| Issue | 结论 | 依据 |
|-------|------|------|
| **01** 门禁 | **通过** | `remix-understanding-gate.ts` + `validateRemixUnderstandingArtifacts`；`sceneforge-remix-understanding-gate.test.ts`；占位不写 approved；关键帧变更 `markRemixUnderstandingStale`（segmentation/keyframe 路径） |
| **02** 音频 | **通过** | `remix-audio-extraction-service.ts`；`sceneforge-remix-audio-extraction.test.ts`（全片+分段 wav/meta） |
| **03c** STT | **通过** | `remix-whisper-provider` + `remix-transcript-service`；`sceneforge-remix-transcript.test.ts`、`sceneforge-remix-whisper-provider.test.ts`、`transcript-aligner` 测试 |
| **04** 片段 LLM | **通过** | `remix-understanding-service` + schema；`sceneforge-remix-understanding.test.ts`（含单段重跑 inputHash）；`segment-understanding-schema` 测试 |
| **05** rollup | **通过** | `remix-source-understanding-rollup` + gate kind；`sceneforge-remix-source-understanding-rollup.test.ts`；理解 E2E 校验 `validation.ok` |

**Issue 01–05 残留（非阻塞）**

- UI 层对 **stale** 的显式 chip/文案可再强化（后端 gate 已有）。
- 03c 实机 whisper 依赖本机 `ggml-small.bin`（已 gitignore）。
- 05 与 01：理解阶段 UI `approved` 仍依赖 `getAssetProcessingStepStatuses` + 真实产物，与门禁一致。

## 7. P2 修复记录（同轮）

- [x] 原片理解 `runAction` 期间 **1.5s 轮询** `getSourceAsset`，同步 `activeProcessingJob.understandingProgress`。
- [x] `.gitignore` 忽略 `tools/local-stt/whisper/*.bin`。
- [x] 编排器进度顶栏 message 与 `understandingProgress` 统一为 `completed/total`。
- [x] 编排器单测补充 understanding phase 进度递增断言。

