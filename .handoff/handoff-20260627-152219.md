# Handoff — Remix 原片理解实机修复 + 首页最近项目 + LLM 注入

> **接替**：[`handoff-20260627-061500.md`](handoff-20260627-061500.md)（Issue 06/07 收口）· **代码基线** git `c80e1c0` + **本工作区未提交补丁**（见 §3）  
> **日期**：2026-06-27  
> **状态**：Issue **01–07 已提交**（`c80e1c0`）；实机阻塞项（Whisper 路径、LLM 配置、最近项目丢失）已在工作区修复，**待重启 dev 验证与 commit**

---

## 0. 相对旧 handoff 的 Errata

| 文档 | 过时说法 | 当前权威 |
|------|----------|----------|
| [`handoff-20260626-215005.md`](handoff-20260626-215005.md) | Issue 01–07 **pending**、理解为占位 | **已实现并提交**至 `c80e1c0`（含 docs/scratch 包） |
| [`handoff-20260627-061500.md`](handoff-20260627-061500.md) | 06/07 **未 git commit** | **已 commit** `c80e1c0`；061500 中 P2（轮询/gitignore/进度文案）亦在该提交内 |
| 184300 / 215005 | 点「生成原片理解」即可测 LLM | 须 **Whisper 可执行文件+模型** + **主进程注入 AISettings**；此前 Remix IPC 未接 LLM，会误报「未配置 LLM」 |
| — | 首页项目「被改没了」 | **非首页 UI 删除**；`recent-projects.json` 曾在 **load=refresh+写盘** 时静默剔除 `existsSync===false` 的路径；已改行为（§3.2） |

---

## 1. 已完成任务与功能（可视为已交付）

### 1.1 原片理解 Issue 01–07（git `c80e1c0` 及更早提交）

| Issue | 状态 | 能力摘要 |
|-------|------|----------|
| **01** 门禁 | **done** | `remix-understanding-gate`、占位不得 approved、关键帧/分段变更 stale |
| **02** 音频 | **done** | 全片/分段 wav + meta，`remix-audio-extraction-service` |
| **03c** 本地 Whisper | **done** | `RemixLocalWhisperProvider`、`remix-transcript-service`、对齐分段台词 |
| **04** 片段 LLM | **done** | `segment_understanding.json` + schema + 结构化生成（可注入 mock） |
| **05** rollup | **done** | `source_overview` / `original_understanding`、gate `validation.ok` |
| **06** 工作台 | **done** | `UnderstandingWorkbenchPanel`、`getSourceUnderstandingWorkbench`、`rerunSegmentUnderstanding`、标注预填 |
| **07** 编排 | **done** | `RemixUnderstandingOrchestrator`、`runSourceUnderstanding` 单 job；台词 fresh 跳过 whisper；理解并发默认 2 |

**一键流程（设计权威）**：`runSourceUnderstanding` →（缺则）抽音频 →（非 fresh 则）全片 whisper+对齐 → 片段理解（并发）→ rollup。

**文档包（同提交）**：`.scratch/sceneforge-remix-source-understanding/*`、`docs/sceneforge-remix/*`（索引见 [`ISSUE_INDEX.md`](../.scratch/sceneforge-remix-source-understanding/ISSUE_INDEX.md)）。

### 1.2 Remix 资产处理与其它已存在能力（延续前序 handoff）

- 手动关键帧 / `KeyframeGallery`、分段、策略/设计/Seedance 等 Remix 管线（见历史 handoff 与 vitest 覆盖面）。
- SceneForge 工坊 LLM：主进程 `loadFullHeadlessAISettings(userData)` — **Remix 现已对齐该路径**（§3.3，未提交）。

### 1.3 本会话工作区补丁（2026-06-27 对话，**未 commit**）

| 主题 | 问题 | 修复 |
|------|------|------|
| **Whisper 路径** | `dist-electron` 下 `repoRoot()` 指到 `Documents/`，报缺 `ggml-small` | [`remix-whisper-provider.ts`](../electron/sceneforge/remix/remix-whisper-provider.ts) 多根解析 + 测试 |
| **最近项目** | 每次 `load-recent-projects` 曾 `refresh` 写盘并剔除失效路径；打开失败会 `removeRecentProject` | [`recent-projects.ts`](../electron/recent-projects.ts)、[`main.ts`](../electron/main.ts)、[`App.tsx`](../src/App.tsx)、[`ProjectList.tsx`](../src/components/ProjectList.tsx)：`load`=只读；`missing` 灰显；`.bak` 备份 |
| **Remix LLM** | `RemixUnderstandingService` 默认 `loadAISettings → null` | [`remix-ai-settings.ts`](../electron/sceneforge/remix/remix-ai-settings.ts) + [`remix-ipc.ts`](../electron/sceneforge/remix/remix-ipc.ts) 注入 `loadFullHeadlessAISettings` |

---

## 2. 验证记录

### 2.1 已跑（工作区）

```bash
npx vitest run tests/sceneforge-remix-whisper-provider.test.ts    # 含 dist-electron 路径回归
npx vitest run tests/recent-projects.test.ts                      # 7 passed（含 missing 保留、.bak）
npx vitest run tests/sceneforge-remix-understanding.test.ts       # 含 loadRemixUnderstandingAISettings 空配置用例
```

`npx tsc --noEmit`：会话内曾跑，主进程改动后请 **重启 dev 前再跑一遍**。

### 2.2 未做 / 待用户

- `./dev.sh` 全链路：**关键帧完成 → 生成原片理解**（Whisper + 多段 LLM + workbench 卡片 + 磁盘产物）。
- 长任务（如 26 段）进度条与 `understandingProgress` 实机观感。
- `tests/sceneforge-remix-ipc-contract.test.ts` 全量跑通可能受 **本机 whisper 子进程/Metal** 影响（与 LLM 注入无关）。

---

## 3. 未提交改动清单（建议下一 agent 先 `git status`）

```
 M electron/main.ts
 M electron/recent-projects.ts
 M electron/sceneforge/remix/remix-ipc.ts
 M electron/sceneforge/remix/remix-whisper-provider.ts
 M src/App.tsx
 M src/components/ProjectList.*
 M src/lib/electron-api.ts
 M tests/recent-projects.test.ts
 M tests/sceneforge-remix-understanding.test.ts
 M tests/sceneforge-remix-whisper-provider.test.ts
?? electron/sceneforge/remix/remix-ai-settings.ts
```

**勿提交**：`tools/local-stt/whisper/ggml-small.bin`（`.gitignore` 已含 `*.bin`）。

**建议 commit 拆分**：

1. `fix(remix): resolve whisper assets when main runs from dist-electron`
2. `fix(app): preserve missing recent projects and stop load-time JSON rewrite`
3. `fix(remix): load global AI settings for segment understanding in main process`

---

## 4. 仍存在的问题 / 风险

| 级别 | 项 | 说明 |
|------|-----|------|
| **P1** | 实机 E2E 未签字 | 用户已遇 Whisper / LLM 报错；补丁逻辑正确但未在本会话完成端到端确认 |
| **P2** | 最近列表历史数据 | 若 `recent-projects.json` 已被刷短，只能从 **`.bak`** 或 Time Machine 手工合并路径；新逻辑防止再丢 |
| **P2** | Whisper 实机稳定性 | CI/本机可能出现 `Whisper 进程退出码：null`（Metal/资源）；与路径解析是不同类问题 |
| **P3** | `understandingConcurrency` | 仍仅 `RemixServiceOptions`，未接全局设置 |
| **P3** | UI stale chip | 后端 gate 有 stale，首页/处理页文案可再强化 |
| **P3** | Remix telemetry | 慢任务诊断仍应优先补 `auto-run` 式 jsonl（见 [`AGENTS.md`](../AGENTS.md)） |

---

## 5. 未完成 / 待设计 / backlog（Issue 包外或边缘）

| ID | 内容 | 状态 |
|----|------|------|
| **03** | Bcut / 云端 ASR fallback | **未做**（主线为 03c 本地 whisper） |
| **03b** | 导入 SRT 快路径 | **backlog**（专篇标边缘） |
| **ui-recovery #16** 等 | 处理工作台 job 与顶栏进度长期一致性 | 06/07 已做 1.5s 轮询；是否还需统一 job 模型待产品确认 |
| **菜单** | 显式「刷新最近项目元数据」调用 `refresh-recent-projects` | 未做（可选） |
| **发布/打包** | `resources/local-stt/whisper` 随应用分发策略 | 文档有述；当前 dev 依赖 `tools/local-stt` |

设计检索入口不变：[`.scratch/.../ISSUE_INDEX.md`](../.scratch/sceneforge-remix-source-understanding/ISSUE_INDEX.md)。

---

## 6. 建议下一 agent 动作（按序）

1. `npx electron-vite build` 或 `./dev.sh` 重启 Electron（主进程 IPC + whisper + recent-projects 均依赖新 bundle）。
2. 实机：打开 Remix 工程 → **生成原片理解** → 确认无 Whisper/LLM 报错 → 检查 `segment_understanding.json`、`source_overview.json`、工作台卡片。
3. 首页：确认「本地草稿」列表行为；缺失目录应 **目录缺失** 灰显而非静默消失。
4. `git status` → 按 §3 拆分 commit；提交前 [`codereview`](../.codereview) 或自审改动面。
5. 若 LLM 仍失败：读 `~/Library/Application Support/灵机剪影/settings.json` 中 `aiSettings`，对照 `getAISettingsIssue`（provider/model/key）。

---

## 7. 建议技能

| 场景 | 技能路径 |
|------|----------|
| 实机/E2E 仍失败 | [`diagnosing-bugs`](/Users/tangwujun/.agents/skills/engineering/diagnosing-bugs/SKILL.md) |
| 提交前审查 | [`codereview`](/Users/tangwujun/.agents/skills/engineering/codereview/SKILL.md) |
| 一键成稿/流水线慢 | 项目 [`AGENTS.md`](../AGENTS.md) auto-run jsonl |
| 继续 Issue/backlog | [`implement`](/Users/tangwujun/.agents/skills/engineering/implement/SKILL.md) + ISSUE_INDEX |

---

## 8. 关键路径速查

| 用途 | 路径 |
|------|------|
| 原片理解编排 | [`electron/sceneforge/remix/remix-understanding-orchestrator.ts`](../electron/sceneforge/remix/remix-understanding-orchestrator.ts) |
| 片段 LLM | [`electron/sceneforge/remix/remix-understanding-service.ts`](../electron/sceneforge/remix/remix-understanding-service.ts) |
| Remix AI 配置 | [`electron/sceneforge/remix/remix-ai-settings.ts`](../electron/sceneforge/remix/remix-ai-settings.ts) |
| Whisper | [`electron/sceneforge/remix/remix-whisper-provider.ts`](../electron/sceneforge/remix/remix-whisper-provider.ts) |
| 最近项目 | [`electron/recent-projects.ts`](../electron/recent-projects.ts) |
| 处理页 UI | [`src/sceneforge/remix/pages/RemixAssetProcessing.tsx`](../src/sceneforge/remix/pages/RemixAssetProcessing.tsx) |
| 用户数据 | `~/Library/Application Support/灵机剪影/`（`settings.json`、`recent-projects.json`、`.bak`） |
| 本地 whisper | [`tools/local-stt/whisper/`](../tools/local-stt/whisper/)（`main` + `ggml-small.bin` 本机放置） |
